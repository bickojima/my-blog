# 2nd release candidate gate results

- Candidate source: `main=f60b81d` merged with `staging=2bbac7d4132a2fcdc6a26a66b8473ab236128b07`; isolated branch `codex/release-2-issues-127-130`.
- Initial three conflicts on `astro.config.mjs`, `public/admin/config.yml`, and deleted `public/robots.txt` were resolved to the staging dynamic configuration. No unresolved paths remain. Parent reviewer independently confirmed the candidate has zero differences from staging for those environment files, `src/content`, `public/images/uploads`, and `public/admin/url-map.json`.
- Lockfile install: `npm ci` in this worktree replaced stale preexisting `node_modules`. Before it, `npm test` used Vitest 4.0.18 and two build tests failed loading `unified`; after lockfile install, Vitest is 4.1.11, `@astrojs/markdown-remark` is 7.3.1 with `unified` export, and build tests pass. Node v25.6.1 / npm 11.9.0 match the validated implementation worktree. `package-lock.json` did not differ from the intended source.
- Tests after install: Vitest 754/754 PASS; `npm run build` pre-step 625/625 PASS; `tests/build.test.mjs` 129/129 PASS; Playwright 457 PASS + 8 existing skipped / 465, 0 failed (14.3 minutes).
- Full Playwright first attempt stopped before tests because sandbox denied `webServer` binding on `0.0.0.0:4173` (EPERM). The authorized retry with only that command elevated ran the complete suite. This was an execution permission issue, not a test result.
- Branch build: `evidence/2026-09-23/release-main/verify-branch-builds.mjs` builds production and staging from the same source and asserts robots, canonical, and sitemap origins. Both PASS; JSON is adjacent in `branch-build-results.json`. After these checks, the final local `dist` was rebuilt with `CF_PAGES_BRANCH=main` and immediately confirmed to contain robots Allow + production sitemap, production canonical, and production-only sitemap URLs.
- A transient earlier read of `dist` showed staging values after a main build followed by a Vitest invocation that ran `build.test.mjs` with default environment and rebuilt `dist`. Repeating the main build and reading output immediately passes. This was an observation-order mix-up; no code defect was established. Avoid using `dist` after a later test that rebuilds it as evidence of the prior build.
- Live staging delivery: environment fingerprint/config, robots, canonical, sitemap, admin CMS config/loader, and CSP verified in `issue127/staging-live/`. Pages deployment commit metadata could not be queried because no Cloudflare API token is configured; no token was searched for.
- Live staging comprehensive E2E: 50 unique scenario IDs on PC/iPad/iPhone (50 each, 150 total), all PASS, with OAuth/GitHub APIs mocked and writes blocked. Results/report/screenshots are in `issue127/staging-comprehensive/`. CMS live-host E2E: 78/78 PASS under `issue130-post-staging/`.
- No main merge or production deploy was performed. GitHub PR diff count must be checked after opening because this release candidate includes the accumulated staging history/evidence; current staged diff from main is 734 files.
